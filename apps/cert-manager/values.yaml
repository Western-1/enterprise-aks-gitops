# CRDs installed by the chart (no separate kubectl apply step).
crds:
  enabled: true
controller:
  resources:
    requests:
      cpu: 100m
      memory: 128Mi
webhook:
  resources:
    requests:
      cpu: 50m
      memory: 64Mi
cainjector:
  resources:
    requests:
      cpu: 50m
      memory: 64Mi